# Tests

This directory is reserved for automated tests.

For Workshop 1, manual validation is performed by running the notebook top-to-bottom and verifying each checkpoint cell passes.

Automated tests covering connectivity, index creation, vector search correctness, and routing behavior may be added in a future iteration.
